local class  = require("tools.class")
Class        = class.declare
New          = class.new
Extends      = class.extends
Delete       = class.delete
IsValid      = class.isValid
IsInstanceOf = class.isInstanceOf

local util = require 'tools.utility'
dump = util.dump
